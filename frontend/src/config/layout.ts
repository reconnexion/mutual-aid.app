/** Shared height for every top bar (the sidebar's branding header and each panel's page header),
 *  so they stay visually aligned across the whole app. */
export const HEADER_HEIGHT = 64;

/** Shared avatar size next to ads and comments, so both look consistent. */
export const AVATAR_SIZE = 40;
